// import {Redis} from "ioredis";
// import { redis } from "redis";
// /**
//  * @method lrange - LRANGE will return specified elements of the list stored at the key.
//  */
// export const purgeSession = async (userId, redis) => {
//     const sessionIds = await redis.lrange(
//         `${}${userId}`
//     )
// }
